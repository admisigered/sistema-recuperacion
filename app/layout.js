import './globals.css'

export const metadata = {
  title: 'SIGERED - Sistema de Recuperación',
}

export default function RootLayout({ children }) {
  return (
    <html lang="es">
      <body>{children}</body>
    </html>
  )
}
